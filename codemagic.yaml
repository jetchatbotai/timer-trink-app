workflows:
  android:
    name: Build APK
    environment:
      node: 20
      java: 17

    scripts:
      - npm install
      - npx cap add android
      - npx cap sync android
      - cd android
      - ./gradlew assembleDebug

    artifacts:
      - android/app/build/outputs/**/*.apk
